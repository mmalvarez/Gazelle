theory Dom_Option
  imports "../Dominant"
begin

  (* We could probably prove this (given a dominant assumption on the
     data inside the option), but since we don't use option this way it's not needed currently.
  *)

end