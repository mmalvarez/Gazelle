theory Semantics imports Syntax
begin

locale gensym'_sem =
fixes beval :: "'g \<Rightarrow> 'b \<Rightarrow> 'st \<Rightarrow> 'st"
fixes 
  (* fix a semantics for 'b *)
  (* fix a semantics for rec
     - this will take 'r as a parameter
       will do what Elle does over the list *)

end
